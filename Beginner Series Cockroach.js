// The cockroach is one of the fastest insects. Write a function which takes its speed in km per hour and returns it in cm per second, rounded down to the integer (= floored).

// For example:

// 1.08 --> 30
// Note! The input is a Real number (actual type is language dependent) and is >= 0. The result should be an Integer.

// Hamam böceği en hızlı böceklerden biridir. Hızını saatte km olarak alan ve tamsayıya (= döşemeli) yuvarlayarak saniyede cm cinsinden döndüren bir fonksiyon yazın.

// Örneğin:

// 1.08 --> 30
// Not! Girdi bir Gerçek sayıdır (gerçek tür dile bağlıdır) ve >= 0'dır. Sonuç bir Tamsayı olmalıdır.

function cockroachSpeed(s){

}


console.log(cockroachSpeed(1.08));